import http, { get, post, put, del } from "./http";
import api, { authApi, userApi, tableApi, formApi, dashboardApi } from "./api";

export {
  http,
  get,
  post,
  put,
  del,
  api,
  authApi,
  userApi,
  tableApi,
  formApi,
  dashboardApi,
};

export default api;
